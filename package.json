{
  "private": true,
  "scripts": {
    "prod:build": "dotenv -e .env.prod -- turbo prod:build",
    "prod:pub": "dotenv -e .env.prod -- turbo prod:pub",
    "dev": "dotenv -e .env.dev -- turbo dev",
    "dev:build": "dotenv -e .env.dev -- turbo dev:build",
    "dev:deploy": "dotenv -e .env.dev -- turbo dev:deploy",
    "dev:nns": "turbo dev:nns",
    "dev:gen": "turbo dev:gen",
    "test": "dotenv -e .env.dev -- turbo test",
    "cargo:repair": "turbo cargo:repair"
  },
  "dependencies": {
    "@dfinity/identity": "0.19.2",
    "@dfinity/agent": "^0.19.2",
    "@dfinity/candid": "^0.19.2",
    "@dfinity/principal": "^0.19.2",
    "zod": "^3.22.2"
  },
  "devDependencies": {
    "glob": "^10.3.4",
    "typescript": "^5.2.2"
  }
}